extends layout.pug

block content
    .container
        h1= article.title
        p= article.content
        div#article-data(data-article=JSON.stringify(article))

        p Applaudissements: #{article.applauseCount}

        button#applaudButton.btn.btn-primary Applaudir

    script(src="/js/applause.js")
