extends layout.pug

block content
  h1 Créer un article

  form(action="/api/articles/create" method="POST")
    div.form-group
      input.form-input(type="text" name="title" placeholder="Titre")
      textarea.form-input(name="content" placeholder="Contenu")
    button.btn.btn-primary(type="submit") Créer
